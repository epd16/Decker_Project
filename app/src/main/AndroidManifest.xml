<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it472.usna.edu.decker_project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity android:name=".CreateEvent1" />
        <activity android:name=".ViewEvents1" />
        <activity android:name=".ViewContacts1" />
        <activity android:name=".CreateEvent2" />
        <activity android:name=".CreateEvent3" />
        <activity android:name=".ViewEvents2" />
        <activity android:name=".ViewContacts2" />
        <activity android:name=".NewContact"></activity>
    </application>

    <uses-permission android:name="android.permission.SEND_SMS" />

</manifest>